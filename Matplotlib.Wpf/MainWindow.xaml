<Window x:Class="Matplotlib.Net.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Matplotlib.Net"
        mc:Ignorable="d"
        SizeToContent="Manual"
        Title="Matplotlib embedded in WPF - DEMO" Height="600" Width="800">
        <Grid x:Name="Grid">
            <Grid.RowDefinitions>
                <RowDefinition Name="MplRow" Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border Grid.ColumnSpan="2" Grid.Row="0" BorderBrush="Red">
                <local:MplPanel  FocusManager.IsFocusScope="True"/>
            </Border>
            <TextBox Grid.Column="0" FontSize="18" TextWrapping="Wrap" AcceptsReturn="True" Grid.Row="2" Height="150" x:Name="Script"/>
            <Button Grid.Column="1" Grid.Row="2" Click="ButtonBase_OnClick">
                Run python code
            </Button>
        </Grid>
</Window>
